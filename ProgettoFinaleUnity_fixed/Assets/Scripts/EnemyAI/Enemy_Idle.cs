using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy_Idle : BaseState
{
    public Enemy_Idle(string name, StateMachine stateMachine) : base(name, stateMachine)
    {
    }
}
